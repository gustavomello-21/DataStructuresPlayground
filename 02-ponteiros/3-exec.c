/*
Sabemos que a linguagem C não é orientada a objeto, em outras palavras ela não
tem a capacidade de criar classes, mas temos uma estrutura semelhante chamada
de Struct, mas ela não tem o mesmo comportamento. Liste quais são as diferenças
entre uma Classe e uma Struct.
*/

Existe algumas diferenças, a classe ela é instânciada já o struct não.
As classes são abstrações de dados já o struct é um cojunto de dados relacionados a um tipo se assemelha com um objeto
As classes podem ser herdadas de uma para a outra já o struct não pode.
A classe pode encapsular funções ja o struct não pode
